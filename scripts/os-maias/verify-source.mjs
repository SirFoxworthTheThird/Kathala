import assert from "node:assert/strict";import {sourceChapters,narrativeText} from "./source-text.mjs";assert.equal(sourceChapters.length,18);assert(narrativeText.length>1_000_000);assert(!narrativeText.includes("FIM DO PRIMEIRO VOLUME"));console.log({chapters:sourceChapters.length,characters:narrativeText.length,head:sourceChapters[0].text.slice(0,40),tail:sourceChapters.at(-1).text.slice(-40)});
